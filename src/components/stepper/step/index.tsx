import React from 'react';

interface IStepProps {
    icon: string;
    title: string;
    description: string;
}

function Step(props: IStepProps) {
    return (
        <div>
            Step
        </div>
    )
}

export default Step;